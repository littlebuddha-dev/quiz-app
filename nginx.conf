# /etc/nginx/sites-available/quiz-app
# Path: nginx.conf
# Title: Nginx Configuration for VPS

server {
    listen 80;
    server_name your-domain.com; # 実際のドメイン名に変更してください

    # HTTP to HTTPS redirect (SSL設定後に有効化)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:3000; # Next.js (PM2) が動いているポート
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # クライアントのIPアドレスを正常に取得するための設定
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
